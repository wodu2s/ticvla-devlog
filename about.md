---
layout: default
title: 프로젝트 개요
description: TIC-VLA 모델을 SerBot II에 온보드로 올리는 프로젝트의 목표와 시스템 구성.
---

<article class="post">
<header class="post-head">
  <h1>프로젝트 개요</h1>
  <p class="post-summary">Vision-Language-Action 모델을 저사양 온보드 하드웨어에서 실제로 굴리는 것이 목표입니다.</p>
</header>

<div class="post-body">

## 무엇을 하는 프로젝트인가

TIC-VLA는 이미지와 자연어 지시를 함께 받아 **주행 waypoint를 직접 출력하는** Vision-Language-Action 모델입니다. 이 프로젝트는 그 모델을 클라우드가 아니라 **로봇에 탑재된 Jetson급 보드 위에서 직접 돌려**, 실제 복도에서 사람을 피해 주행시키는 것을 목표로 합니다.

1단계 시연 시나리오는 한 문장으로 정리됩니다.

> "앞에 있는 사람을 피해서 복도 끝까지 가라"

이 한 문장이 세 개의 검증 항목으로 나뉩니다.

1. **사람을 인식하는가** — 모델이 생성하는 `<think>` 텍스트에 등장하는가
2. **그래서 방향을 트는가** — 출력 waypoint의 횡방향 성분이 바뀌는가
3. **끝에 도달하면 멈추는가** — 예측 변위가 수렴하면 정지 판정이 내려지는가

세 항목 모두 실차에서 확인했습니다. 2단계 목표는 **"저 물체까지 가라"** — 회피가 아니라 지정된 목표 객체로의 접근입니다.

## 하드웨어와 소프트웨어

| 항목 | 내용 |
|---|---|
| 로봇 | SerBot II — 3륜 옴니휠, CAN 버스 모터 제어 |
| 연산 | 온보드 Jetson 계열, 16GB 통합 메모리 |
| 모델 | InternVL3-1B 기반 TIC-VLA (VLM 1.8GB + checkpoint 1.9GB) |
| 미들웨어 | ROS 2 |
| 카메라 | CSI 카메라 1대 (1280×720) |

메모리는 병목이 아니었습니다. CUDA 할당 1.8GB, RSS 1.7GB로 16GB 안에서 여유가 충분했고, 양자화나 외부 GPU 서버는 초기 단계에서 불필요했습니다. **진짜 병목은 처음부터 끝까지 지연시간이었습니다.**

## 구조 — 느린 루프와 빠른 루프

VLM 한 번 추론에 20초 이상이 걸리기 때문에, 추론과 제어를 같은 주기로 돌릴 수 없습니다. 그래서 둘을 분리했습니다.

```
느린 루프 (약 30초)   이미지 → VLM → <think> 텍스트 생성 → KV 캐시 갱신
빠른 루프 (1Hz)      현재 이미지 + 캐시된 의미정보 + 로봇 상태
                     → Action Expert → waypoint → 속도 명령
```

Action Expert 단독 실행은 11.9ms입니다. 즉 **빠른 루프는 얼마든지 빠르게 돌릴 수 있고**, 느려지는 것은 장면을 새로 해석하는 주기뿐입니다.

## 안전 계층

모델이 내는 명령을 모터에 바로 넣지 않습니다. 사이에 우선순위 기반 관문 노드를 두었습니다.

```
비상정지(래치) > 조이스틱 > 비활성화 > 명령 타임아웃 > 정상 통과
```

상위 노드가 강제 종료돼도 0.5초 안에 로봇이 멈추도록 타임아웃을 두었고, 후진은 아예 클램프로 차단합니다. 카메라가 앞만 보고 있으므로 후진은 곧 시야 밖으로 움직이는 것이기 때문입니다.

## 원본 연구

이 프로젝트는 아래 연구를 기반으로 재현·구현한 것입니다.

- 논문 코드: [ucla-mobility/TIC-VLA](https://github.com/ucla-mobility/TIC-VLA)
- 학습 데이터셋: SCAND (보행자 환경 사회적 주행 데이터)

</div>
</article>
