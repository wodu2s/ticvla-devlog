---
layout: default
title: 데이터셋
description: GoToObject-v1 — 목표 객체 도달 태스크의 조종 시연 데이터셋 구성과 수집 방법.
permalink: /dataset/
---

<article class="post">
<header class="post-head">
  <h1>GoToObject-v1</h1>
  <p class="post-summary">"저 물체까지 가서 멈춰라"를 가르치기 위한 조종 시연 데이터셋. 구성과 수집 방법.</p>
</header>

<div class="post-body" markdown="1">

<div class="todo-meta">
2026-08-19 기준 · <strong>수집 예정</strong> · 수집 완료 후 실측값으로 갱신
</div>

## 왜 모으는가

사전학습 모델은 보행자 환경의 사회적 주행 데이터로 학습됐다. 그 데이터에서 모든 물체는 **피해야 할 것**이었고, "무언가를 향해 가서 멈춘다"는 궤적은 거의 없었다.

파인튜닝 전 기준선을 관측 모드에서 재보면 그 성향이 그대로 드러난다.

| 물체 배치 | 6초 시점 횡방향 (+ 가 왼쪽) | 해석 |
|---|---|---|
| 좌측 | **−0.28** | 오른쪽으로 간다 |
| 우측 | **+0.65** | 왼쪽으로 간다 |

**물체가 있는 쪽의 반대로 간다.** 그리고 목표를 지정하든 안 하든 값이 거의 같다. 지시문은 방향을 만들지 못한다.

여기에 하나 더. 로봇이 목표에 다가가는데도 **예측 변위가 줄지 않는다.** 1.4~2.2 사이에서 계속 진동한다. 도착이라는 개념이 모델에 없다는 뜻이고, 그래서 구조적으로 멈출 수가 없다.

이 데이터셋이 가르쳐야 할 것은 두 가지다.

| | 지금 | 학습 후 |
|---|---|---|
| 좌측 배치 | −0.28 (반대) | **+ 로 부호 전환** |
| 우측 배치 | +0.65 (반대) | **− 로 부호 전환** |
| 예측 변위 | 접근해도 안 줄어듦 | 가까워질수록 감소 |

미세한 개선이 아니라 **부호 반전**이다.

## 태스크

목표 객체는 **하나**(회색 통). 좌 · 정면 · 우 세 위치에 번갈아 놓고, 로봇은 항상 가운데에서 출발한다. 태스크 이름은 **객체가 놓인 위치**를 뜻한다.

| task | 객체 위치 | 지시문 |
|---|---|---|
| `L` | 좌측 | 세 태스크 동일 |
| `C` | 정면 | 세 태스크 동일 |
| `R` | 우측 | 세 태스크 동일 |

객체가 하나뿐이라 지시문은 셋 다 같다. **따라서 이 단계로는 "지시문을 따랐다"를 증명할 수 없다.** 두 번째 객체를 추가하고 둘을 같은 화면에 두는 다음 단계에서 가능해진다.

좌우 배치는 각도를 재지 않고 **화면을 보고** 맞춘다. 객체 중심이 화면 폭의 25~30% 지점. 초기에 25도를 의도했다가 실제로는 8도밖에 안 옮겨져서 정면과 구분되지 않은 적이 있다.

## 에피소드 구조

```
접근 (0.10 m/s)  →  1.5m 부터 감속  →  0.8m 정지  →  정지 유지 8초
```

| 시작 거리 | 접근 | 정지 유지 | 총 길이 |
|---|---|---|---|
| 2.0 m | 약 20초 | 8초 | 약 30초 |
| 3.0 m | 약 30초 | 8초 | 약 40초 |

### 정지 8초가 핵심이다

접근 궤적만 모으면 모델은 "항상 앞으로"만 배운다. **목표 앞에 선 채로 계속 기록해야** "도달 = 변위 0"이 학습된다. 위에서 본 "예측 변위가 안 줄어드는" 증상이 정확히 그 신호가 없어서 생긴 것이다.

8초는 계산된 값이다. 10Hz 기준 80프레임이고 에피소드 전체의 약 20%. 더 길면 멈추는 쪽으로 치우치고, 짧으면 수렴을 못 배운다.

감속도 같은 이유다. 급정지가 아니라 1.5m부터 서서히 줄여야 **"가까울수록 변위가 작다"는 연속적인 신호**가 생긴다.

### 궤적 규칙

```
✗ S자                        ✓ 대각선 직행

  [통]                         [통]
   ↖                            ↑   ← 마지막 1m 정면 접근
     ＼                         ╲
       ╲___                      ╲
           ＼                     ╲  ← 출발 직후부터 통 쪽으로
         ／                        ╲   몸을 돌리며 전진
       ／                           ╲
    [로봇]                        [로봇]
```

- **출발 직후부터** 목표 쪽으로 부드럽게 튼다. 직진하다 끝에서 꺾으면 대부분의 프레임이 "가운데 있으니 직진"이 되어 학습 신호가 얇아진다
- **옆으로만 미끄러지는 이동(게걸음)은 쓰지 않는다.** 몸이 안 돌면 도착해도 물체가 화면 가장자리에 남는다. 실차에서 모델이 낸 명령이 정확히 그 형태(회전 0 + 횡방향 드리프트)였고, 우리 데이터에 회전이 있어야 모델이 회전을 배운다
- 정지 시점에 **물체가 화면 중앙**에 오도록
- 경로가 조금 흔들리는 것은 무방하다. **끝만 일정하면 된다**

## 수집 규모

```
task L 10개  +  task C 10개  +  task R 10개  =  30 에피소드
```

- 각 10개 안에서 시작 거리 2.0m 다섯 개 / 3.0m 다섯 개
- **시작 위치를 매 반복 30~50cm 변동.** 완전히 고정하면 모델이 궤적을 통째로 외운다
- 30개 중 일부는 **배경이 다른 방향**에서 수집한다. 물체 뒤가 항상 벽이면 "벽이 가까우면 멈춘다"를 배울 수 있다

20초 창을 2초씩 밀며 자르면 약 **250개 학습 샘플**이 나온다. 1차 파이프라인 검증에는 충분하고, 학습곡선을 보고 증량을 판단한다.

## 무엇을 저장하는가

한 에피소드는 **이미지 폴더와 궤적 CSV 두 개**다. 나머지는 학습 파이프라인이 만든다.

```
ep0017_graybin_L_2m/
├── frames/000000.jpg …          10 Hz
└── trajectory.csv               time, x, y, z, qx, qy, qz, qw
```

- 폴더명: `ep<번호>_<객체>_<task>_<시작거리>`
- **이미지 1장 = CSV 1행.** 인덱스가 1:1로 대응한다
- 위치와 자세는 **휠 오도메트리 실측값**을 변환 없이 그대로 쓴다. 좌표계가 이미 표준(x 전방+, y 좌측+)과 같다
- 속도 명령을 적분한 추정 경로는 별도 파일로 따로 남긴다. 학습에는 쓰지 않고, 명령과 실제가 얼마나 벌어졌는지 비교하는 용도다

### 파이프라인

```
이미지 + trajectory.csv
   │  20초 윈도우 분할 (2초씩 슬라이드)
   │  로봇 중심 상대 좌표로 변환
   ▼
윈도우 JSON  ── 과거 관측 9초 + 미래 궤적 3초
   │  지시문 부착
   │  궤적과 이미지로부터 사고 과정 텍스트 생성
   ▼
학습 샘플
```

예측 지평은 **미래 3초 = 30스텝 @10Hz**, 관측 창은 **9초 = 90스텝**이다. "3초/6초/9초 웨이포인트"는 우리가 계산하는 게 아니라 상대 증분을 누적한 결과물이다.

## 수집 규칙

- 조종 속도 상한 **0.10 m/s** 엄수. 실기 추론과 스케일을 맞추기 위해서다. 빠르게 몰면 데이터가 쓸모없어진다
- 풀스틱일 때 정확히 0.10이 되도록 스케일을 잡는다. 중간 지점에서 속도를 맞추려 하면 손떨림이 그대로 데이터에 들어간다
- **휠 오도메트리 필수.** 없으면 기록을 시작하지 않도록 코드에 막아두었다
- 목표를 **화면 안에 유지**한다. 프레임 밖으로 나가면 그 에피소드는 버린다
- 매일 첫 에피소드는 버린다 (조작 워밍업)

## 검수

에피소드마다 종료 시 자동으로 확인한다.

```
프레임 수 == CSV 행 수
프레임 간격 중앙값 100ms 근처
오도메트리 수신 건수 > 0
마지막 3초 이동거리 < 0.03m      ← 정지 유지가 제대로 들어갔는가
총 이동거리 > 0.3m
```

하나라도 걸리면 경고를 띄우고 버릴지 판단한다. 10개마다 저장된 프레임을 열어 목표가 계속 보였는지도 확인한다.

## 한계

| 항목 | 내용 |
|---|---|
| 단일 객체 | 지시문이 항상 같아 **"지시를 따랐다"를 증명할 수 없다.** 두 번째 객체를 추가하는 단계에서 가능해진다 |
| 조명 1조건 | 반나절 수집의 필연 |
| 장소 1곳 | 배경 다양성이 시작 위치 변경으로만 확보된다 |
| 30 에피소드 | 이 분야 기준으로는 적다. **1차 학습의 목적은 성능이 아니라 파이프라인 관통과 방향 확인**이다 |

1차 학습의 성공 기준은 성공률이 아니다. 기준선 대비 **① 횡방향 부호가 목표 쪽으로 이동했는가 ② 접근할수록 예측 변위가 줄어드는가.** 이 둘이 숫자로 보이면 성공이다.

</div>
</article>
